const { convertToJsonSchema } = require("./convert-to-json-schema");
const { separateColumnsByTable } = require("./separate-columns-by-table");

module.exports = { convertToJsonSchema, separateColumnsByTable };
